"""Namespace marker for the ``monorepo`` proto root.

The ``v1`` sub-package below is populated by ``buf generate``. This file
is committed so a fresh checkout still has a valid Python package tree
before the generator has been run.
"""
from __future__ import annotations
