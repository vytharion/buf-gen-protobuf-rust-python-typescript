/**
 * Namespace marker for the `monorepo` proto root.
 *
 * The `v1` sub-folder below is populated by `buf generate`. This file
 * is committed so a fresh checkout still has a valid TypeScript module
 * tree before the generator has been run.
 */

export {};
